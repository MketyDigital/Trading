name: Temporary Runtime Control Diagnostics

on:
  push:
    branches:
      - fix/frontend-runtime-controls

permissions:
  contents: read

jobs:
  isolate-node-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cloudflare-v2
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install --no-audit --no-fund
      - name: Run every Node test file independently
        shell: bash
        run: |
          failed=0
          for file in tests/*.test.mjs; do
            echo "=== $file ==="
            log="$RUNNER_TEMP/$(basename "$file").log"
            if node --test "$file" >"$log" 2>&1; then
              echo "PASS $file"
            else
              echo "::error file=$file,title=FAILED_TEST::$file"
              cat "$log"
              failed=1
            fi
          done
          exit "$failed"
