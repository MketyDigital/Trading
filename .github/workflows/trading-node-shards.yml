name: Trading Node Shards

on:
  pull_request:
    paths:
      - 'cloudflare-v2/src/**'
      - 'cloudflare-v2/tests/**'
      - '.github/workflows/trading-node-shards.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  shard:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: a-c
            pattern: 'tests/[a-c]*.test.mjs'
          - name: d-f
            pattern: 'tests/[d-f]*.test.mjs'
          - name: g-i
            pattern: 'tests/[g-i]*.test.mjs'
          - name: j-l
            pattern: 'tests/[j-l]*.test.mjs'
          - name: m-o
            pattern: 'tests/[m-o]*.test.mjs'
          - name: p-r
            pattern: 'tests/[p-r]*.test.mjs'
          - name: s-u
            pattern: 'tests/[s-u]*.test.mjs'
          - name: v-x
            pattern: 'tests/[v-x]*.test.mjs'
          - name: y-z
            pattern: 'tests/[y-z]*.test.mjs'
          - name: full-serial
            pattern: '--test-concurrency=1 tests/*.test.mjs'
    name: node-${{ matrix.name }}
    defaults:
      run:
        working-directory: cloudflare-v2
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install --no-audit --no-fund
      - name: Run shard
        shell: bash
        run: node --test ${{ matrix.pattern }}
